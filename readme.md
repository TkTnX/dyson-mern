# Dyson MERN - Интернет-магазин техники Dyson

## 📋 Описание проекта

Это полнофункциональный интернет-магазин для продажи техники Dyson, построенный на стеке MERN (MongoDB, Express, React, Node.js). Проект включает в себя каталог товаров с категориями, систему отзывов, новостной раздел, корзину покупок и административные функции для управления контентом.

## 🛠 Технологический стек

### Backend
- **Node.js** - серверная среда выполнения
- **Express.js** (v5.2.1) - веб-фреймворк
- **MongoDB** (v7.0.0) - база данных
- **Mongoose** (v9.0.2) - ODM для MongoDB
- **Multer** (v2.0.2) - обработка загрузки файлов
- **CORS** (v2.8.5) - настройка CORS
- **dotenv** (v17.2.3) - управление переменными окружения

### Frontend
- **React** (v19.2.0) - UI библиотека
- **TypeScript** (v5.9.3) - типизация
- **Vite** (rolldown-vite v7.2.5) - сборщик и dev-сервер
- **React Router** (v7.11.0) - маршрутизация
- **TanStack React Query** (v5.90.12) - управление серверным состоянием
- **Zustand** (v5.0.9) - управление клиентским состоянием (корзина)
- **Axios** (v1.13.2) - HTTP клиент
- **Tailwind CSS** (v4.1.18) - CSS фреймворк
- **Swiper** (v12.0.3) - слайдеры
- **Lucide React** (v0.562.0) - иконки

### Инструменты разработки
- **ESLint** - линтер
- **Prettier** - форматирование кода
- **Nodemon** - автоматическая перезагрузка сервера

## 📁 Структура проекта

```
Dyson-mern/
├── backend/                 # Backend приложение
│   ├── controllers/         # Контроллеры (бизнес-логика)
│   │   ├── category.controller.js
│   │   ├── news.controller.js
│   │   ├── product.controller.js
│   │   └── review.controller.js
│   ├── libs/                # Утилиты и вспомогательные функции
│   │   ├── connectToDb.js   # Подключение к MongoDB
│   │   └── upload.js        # Настройка Multer для загрузки файлов
│   ├── models/              # Mongoose модели
│   │   ├── category.model.js
│   │   ├── complectation.model.js
│   │   ├── news.model.js
│   │   ├── product.model.js
│   │   ├── review.model.js
│   │   └── specification.model.js
│   ├── routes/              # API роуты
│   │   ├── category.route.js
│   │   ├── news.route.js
│   │   ├── product.route.js
│   │   ├── review.route.js
│   │   └── upload.route.js
│   ├── uploads/             # Загруженные изображения
│   ├── index.js             # Точка входа сервера
│   └── package.json
│
└── frontend/                # Frontend приложение
    ├── src/
    │   ├── entities/        # Бизнес-сущности (компоненты)
    │   │   ├── CartItem/
    │   │   ├── Category/
    │   │   ├── Complectation/
    │   │   ├── News/
    │   │   ├── Product/
    │   │   ├── Review/
    │   │   └── Specification/
    │   ├── features/        # Функциональные компоненты
    │   │   ├── AddToCartButton.tsx
    │   │   └── ProductCounter.tsx
    │   ├── pages/           # Страницы приложения
    │   │   ├── CategoryPage.tsx
    │   │   ├── Homepage.tsx
    │   │   ├── NewsItemPage.tsx
    │   │   ├── NewsPage.tsx
    │   │   └── ProductPage.tsx
    │   ├── shared/          # Переиспользуемые компоненты и утилиты
    │   │   ├── components/  # UI компоненты
    │   │   │   ├── modals/  # Модальные окна
    │   │   │   ├── sheets/  # Боковые панели
    │   │   │   └── ui/      # Базовые UI компоненты
    │   │   ├── constants/   # Константы
    │   │   ├── helpers/     # Вспомогательные функции
    │   │   ├── hooks/       # Кастомные хуки
    │   │   ├── libs/        # Библиотеки (axios instance)
    │   │   ├── stores/     # Zustand сторы
    │   │   └── types/       # TypeScript типы
    │   ├── widgets/         # Виджеты (крупные компоненты)
    │   │   ├── CategoriesList.tsx
    │   │   ├── ComplectationList.tsx
    │   │   ├── Contact.tsx
    │   │   ├── Delivery.tsx
    │   │   ├── Faq.tsx
    │   │   ├── Footer.tsx
    │   │   ├── Header.tsx
    │   │   ├── Hero.tsx
    │   │   ├── Navbar.tsx
    │   │   ├── News.tsx
    │   │   ├── NewsList.tsx
    │   │   ├── Offers.tsx
    │   │   ├── Opinion.tsx
    │   │   ├── Payment.tsx
    │   │   ├── Reviews.tsx
    │   │   ├── SimilarProductsList.tsx
    │   │   ├── SpecificationsList.tsx
    │   │   └── WhyWe.tsx
    │   ├── main.tsx         # Точка входа React приложения
    │   └── index.css        # Глобальные стили
    ├── public/              # Статические файлы
    └── package.json
```

## 🗄 Модели данных

### Product (Товар)
- `title` (String, required) - название товара
- `description` (String) - описание
- `price` (Number, required) - цена
- `discount` (Number) - скидка
- `isStock` (Boolean, default: true) - наличие на складе
- `images` ([String]) - массив изображений
- `rating` (Number, default: 0) - рейтинг (вычисляется из отзывов)
- `complectation` ([ObjectId]) - комплектации (ref: Complectation)
- `specifications` ([ObjectId]) - характеристики (ref: Specification)
- `reviews` ([ObjectId]) - отзывы (ref: Review)
- `category` (ObjectId) - категория (ref: Category)
- `created_at`, `updated_at` - временные метки

### Category (Категория)
- `name` (String, required) - название категории
- `slug` (String, required, unique) - URL-слаг
- `image` (String) - изображение категории

### Review (Отзыв)
- `title` (String, required, maxLength: 100) - заголовок отзыва
- `text` (String) - текст отзыва
- `rating` (Number, required, min: 1, max: 5) - оценка
- `images` ([String]) - изображения в отзыве
- `username` (String, required) - имя пользователя
- `product` (ObjectId, required, ref: Product) - товар
- `created_at`, `updated_at` - временные метки

### News (Новость)
- `title` (String, required) - заголовок новости
- `content` (String, required) - содержание новости
- `image` (String) - изображение новости
- `created_at`, `updated_at` - временные метки

### Complectation (Комплектация)
- `image` (String) - изображение
- `title` (String, required) - название
- `description` (String, required) - описание

### Specification (Характеристика)
- `label` (String, required) - название характеристики
- `value` (String, required) - значение

## 🛣 API Роуты

### Products (`/products`)
- `GET /` - получить список товаров
  - Query параметры:
    - `sortBy` - сортировка (например, "price-asc", "price-desc")
    - `important` - фильтр по важным полям
    - `limit` - количество товаров на странице
    - `page` - номер страницы
    - `category` - фильтр по категории (slug)
  - Возвращает: `{ items: Product[], totalProducts: number, totalPages: number }`
- `GET /:productId` - получить товар по ID
  - Возвращает полный объект товара с populate данными
- `POST /` - создать новый товар
  - Body: `{ title, description, price, discount, images, category }`

### Categories (`/categories`)
- `GET /` - получить все категории
  - Возвращает: `Category[]`

### Reviews (`/reviews`)
- `GET /` - получить отзывы
  - Query параметры:
    - `limit` - количество на странице
    - `page` - номер страницы
    - Другие параметры для фильтрации
  - Возвращает: `{ items: Review[], total: number, totalPages: number }`
- `POST /` - создать отзыв
  - Body: `{ title, text, rating, images, username, productId }`
  - Автоматически обновляет рейтинг товара
- `DELETE /:id` - удалить отзыв

### News (`/news`)
- `GET /` - получить список новостей
  - Query параметры:
    - `limit` - ограничение количества
  - Возвращает: `News[]`
- `GET /:id` - получить новость по ID
- `POST /` - создать новость
  - Body: `{ title, content, image }`

### Upload (`/upload`)
- `POST /` - загрузить изображение
  - FormData с полем `image`
  - Возвращает: `{ message: string, filename: string, url: string }`

### Static Files
- `GET /uploads/:filename` - получить загруженное изображение

## 🎨 Frontend Роуты

- `/` - Главная страница (Homepage)
  - Hero секция
  - Список категорий
  - Мнение клиентов
  - Почему мы
  - Предложения
  - Отзывы
  - FAQ
  - Новости
- `/products/:productId` - Страница товара (ProductPage)
  - Детальная информация о товаре
  - Изображения
  - Комплектации
  - Характеристики
  - Отзывы
  - Похожие товары
  - Кнопка добавления в корзину
- `/categories/:slug` - Страница категории (CategoryPage)
  - Список товаров категории
  - Фильтрация и сортировка
- `/news` - Страница новостей (NewsPage)
  - Список всех новостей
- `/news/:id` - Страница новости (NewsItemPage)
  - Детальная информация о новости

## 🔧 Функциональность

### Корзина покупок
- Управление через Zustand store (`useCartStore`)
- Добавление товаров с указанием количества
- Удаление товаров из корзины
- Хранение состояния в памяти (не сохраняется между сессиями)

### Отзывы
- Просмотр отзывов к товарам
- Добавление новых отзывов с рейтингом и изображениями
- Автоматический пересчет рейтинга товара
- Модальные окна для просмотра и добавления отзывов

### Каталог товаров
- Фильтрация по категориям
- Сортировка (по цене, рейтингу и т.д.)
- Пагинация
- Поиск товаров (если реализован)

### Новости
- Просмотр списка новостей
- Детальная страница новости
- Ограничение количества на главной странице

### Загрузка файлов
- Загрузка изображений через Multer
- Сохранение в папку `backend/uploads`
- Возврат URL для доступа к файлу

## 🚀 Установка и запуск

### Требования
- Node.js (версия 18+)
- MongoDB (локально или облачный сервис)
- npm или yarn

### Backend

1. Перейдите в папку backend:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в папке `backend/`:
```env
PORT=5000
DB_URL=mongodb://localhost:27017/dyson-shop
CLIENT_URL=http://localhost:5173
```

4. Запустите сервер:
```bash
npm run start:dev
```

Сервер будет доступен на `http://localhost:5000`

### Frontend

1. Перейдите в папку frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в папке `frontend/`:
```env
VITE_PUBLIC_SERVER_URL=http://localhost:5000
```

4. Запустите dev-сервер:
```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

5. Сборка для production:
```bash
npm run build
```

## 📦 Основные зависимости

### Backend
- `express` - веб-фреймворк
- `mongoose` - ODM для MongoDB
- `multer` - обработка файлов
- `cors` - CORS middleware
- `dotenv` - переменные окружения

### Frontend
- `react` + `react-dom` - React библиотека
- `react-router` - маршрутизация
- `@tanstack/react-query` - серверное состояние
- `zustand` - клиентское состояние
- `axios` - HTTP запросы
- `tailwindcss` - стилизация
- `swiper` - слайдеры
- `lucide-react` - иконки

## 🎯 Особенности архитектуры

### Frontend
- **Feature-Sliced Design** - структура проекта следует принципам FSD
  - `entities` - бизнес-сущности
  - `features` - функциональные возможности
  - `widgets` - крупные составные компоненты
  - `pages` - страницы приложения
  - `shared` - переиспользуемый код
- **TypeScript** - полная типизация
- **React Query** - кэширование и синхронизация данных
- **Zustand** - легковесное управление состоянием
- **Tailwind CSS** - utility-first CSS

### Backend
- **MVC архитектура** - разделение на модели, контроллеры и роуты
- **Mongoose** - валидация и связи между моделями
- **RESTful API** - стандартные HTTP методы
- **Middleware** - CORS, JSON парсинг, статические файлы

## 🔐 Безопасность

- CORS настроен для работы только с указанным клиентом
- Валидация данных на уровне Mongoose схем
- Обработка ошибок в контроллерах
- Проверка существования ресурсов перед операциями

## 📝 Примечания

- Корзина хранится только в памяти браузера (не сохраняется между сессиями)
- Рейтинг товара автоматически пересчитывается при добавлении/удалении отзывов
- Изображения загружаются в папку `backend/uploads` и доступны через статический роут
- Проект использует ES модули (type: "module")

## 🛠 Скрипты

### Backend
- `npm run start:dev` - запуск в режиме разработки с nodemon

### Frontend
- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка для production
- `npm run preview` - предпросмотр production сборки
- `npm run lint` - проверка кода линтером


